describe("renders the add menu page", () => {
    it("menu page can be rendered", () => {
        cy.visit("http://localhost:3000/menuadd");
    });

    /*
    it("creating menu for restaurant can be used", () =>{
        cy.visit("http://localhost:3000/menuadd");
        cy.get("form");

        cy.get('input[')
    });*/
});

